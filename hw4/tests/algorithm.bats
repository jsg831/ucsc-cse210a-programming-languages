load harness

@test "algorithm-pow" {
  check 'x := 3; y := 10; s := 1; while ¬(y = 0) do { s := s * x; y := y - 1}' '⇒ skip; y := 10; s := 1; while ¬(y=0) do { s := (s*x); y := (y-1) }, {x → 3}
⇒ y := 10; s := 1; while ¬(y=0) do { s := (s*x); y := (y-1) }, {x → 3}
⇒ skip; s := 1; while ¬(y=0) do { s := (s*x); y := (y-1) }, {x → 3, y → 10}
⇒ s := 1; while ¬(y=0) do { s := (s*x); y := (y-1) }, {x → 3, y → 10}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 1, x → 3, y → 10}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 1, x → 3, y → 10}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 1, x → 3, y → 10}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 3, x → 3, y → 10}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 3, x → 3, y → 10}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 3, x → 3, y → 9}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 3, x → 3, y → 9}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 3, x → 3, y → 9}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 9, x → 3, y → 9}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 9, x → 3, y → 9}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 9, x → 3, y → 8}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 9, x → 3, y → 8}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 9, x → 3, y → 8}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 27, x → 3, y → 8}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 27, x → 3, y → 8}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 27, x → 3, y → 7}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 27, x → 3, y → 7}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 27, x → 3, y → 7}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 81, x → 3, y → 7}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 81, x → 3, y → 7}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 81, x → 3, y → 6}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 81, x → 3, y → 6}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 81, x → 3, y → 6}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 243, x → 3, y → 6}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 243, x → 3, y → 6}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 243, x → 3, y → 5}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 243, x → 3, y → 5}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 243, x → 3, y → 5}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 729, x → 3, y → 5}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 729, x → 3, y → 5}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 729, x → 3, y → 4}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 729, x → 3, y → 4}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 729, x → 3, y → 4}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 2187, x → 3, y → 4}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 2187, x → 3, y → 4}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 2187, x → 3, y → 3}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 2187, x → 3, y → 3}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 2187, x → 3, y → 3}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 6561, x → 3, y → 3}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 6561, x → 3, y → 3}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 6561, x → 3, y → 2}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 6561, x → 3, y → 2}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 6561, x → 3, y → 2}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 19683, x → 3, y → 2}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 19683, x → 3, y → 2}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 19683, x → 3, y → 1}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 19683, x → 3, y → 1}
⇒ s := (s*x); y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 19683, x → 3, y → 1}
⇒ skip; y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 59049, x → 3, y → 1}
⇒ y := (y-1); while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 59049, x → 3, y → 1}
⇒ skip; while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 59049, x → 3, y → 0}
⇒ while ¬(y=0) do { s := (s*x); y := (y-1) }, {s → 59049, x → 3, y → 0}
⇒ skip, {s → 59049, x → 3, y → 0}'
}

@test "algorithm-fib" {
  check 'x := 0; y := 1; i := 2; while x < 10 do { t := x + y; y := x; x := t }' '⇒ skip; y := 1; i := 2; while (x<10) do { t := (x+y); y := x; x := t }, {x → 0}
⇒ y := 1; i := 2; while (x<10) do { t := (x+y); y := x; x := t }, {x → 0}
⇒ skip; i := 2; while (x<10) do { t := (x+y); y := x; x := t }, {x → 0, y → 1}
⇒ i := 2; while (x<10) do { t := (x+y); y := x; x := t }, {x → 0, y → 1}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, x → 0, y → 1}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, x → 0, y → 1}
⇒ t := (x+y); y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, x → 0, y → 1}
⇒ skip; y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 0, y → 1}
⇒ y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 0, y → 1}
⇒ skip; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 0, y → 0}
⇒ x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 0, y → 0}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 0}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 0}
⇒ t := (x+y); y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 0}
⇒ skip; y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 0}
⇒ y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 0}
⇒ skip; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 1}
⇒ x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 1}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 1}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 1}
⇒ t := (x+y); y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 1, x → 1, y → 1}
⇒ skip; y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 2, x → 1, y → 1}
⇒ y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 2, x → 1, y → 1}
⇒ skip; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 2, x → 1, y → 1}
⇒ x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 2, x → 1, y → 1}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 2, x → 2, y → 1}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 2, x → 2, y → 1}
⇒ t := (x+y); y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 2, x → 2, y → 1}
⇒ skip; y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 3, x → 2, y → 1}
⇒ y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 3, x → 2, y → 1}
⇒ skip; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 3, x → 2, y → 2}
⇒ x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 3, x → 2, y → 2}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 3, x → 3, y → 2}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 3, x → 3, y → 2}
⇒ t := (x+y); y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 3, x → 3, y → 2}
⇒ skip; y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 5, x → 3, y → 2}
⇒ y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 5, x → 3, y → 2}
⇒ skip; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 5, x → 3, y → 3}
⇒ x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 5, x → 3, y → 3}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 5, x → 5, y → 3}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 5, x → 5, y → 3}
⇒ t := (x+y); y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 5, x → 5, y → 3}
⇒ skip; y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 8, x → 5, y → 3}
⇒ y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 8, x → 5, y → 3}
⇒ skip; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 8, x → 5, y → 5}
⇒ x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 8, x → 5, y → 5}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 8, x → 8, y → 5}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 8, x → 8, y → 5}
⇒ t := (x+y); y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 8, x → 8, y → 5}
⇒ skip; y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 13, x → 8, y → 5}
⇒ y := x; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 13, x → 8, y → 5}
⇒ skip; x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 13, x → 8, y → 8}
⇒ x := t; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 13, x → 8, y → 8}
⇒ skip; while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 13, x → 13, y → 8}
⇒ while (x<10) do { t := (x+y); y := x; x := t }, {i → 2, t → 13, x → 13, y → 8}
⇒ skip, {i → 2, t → 13, x → 13, y → 8}'
}

@test "algorithm-gcd" {
  check 'x := 123; y := 789; while ¬(x = y) do { if x < y then y := y - x else x := x - y }' '⇒ skip; y := 789; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123}
⇒ y := 789; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 789}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 789}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 789}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 789}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 666}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 666}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 666}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 666}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 543}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 543}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 543}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 543}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 420}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 420}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 420}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 420}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 297}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 297}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 297}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 297}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 174}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 174}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 174}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 174}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 51}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 51}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 51}
⇒ x := (x-y); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 123, y → 51}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 72, y → 51}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 72, y → 51}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 72, y → 51}
⇒ x := (x-y); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 72, y → 51}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 51}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 51}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 51}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 51}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 30}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 30}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 30}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 30}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 9}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 9}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 9}
⇒ x := (x-y); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 21, y → 9}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 12, y → 9}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 12, y → 9}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 12, y → 9}
⇒ x := (x-y); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 12, y → 9}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 9}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 9}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 9}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 9}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 6}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 6}
⇒ if (x<y) then { y := (y-x) } else { x := (x-y) }; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 6}
⇒ y := (y-x); while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 6}
⇒ skip; while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 3}
⇒ while ¬(x=y) do { if (x<y) then { y := (y-x) } else { x := (x-y) } }, {x → 3, y → 3}
⇒ skip, {x → 3, y → 3}'
}

@test "algorithm-sqrt" {
  check 'y := 123456; x := 0; while ¬(¬ (y < x*x) ∧ y < (x+1)*(x+1)) do { s := 1; xp := x; while (x+s)*(x+s) < y do { xp := x + s; s := s * 2 }; x := xp }' '⇒ skip; x := 0; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {y → 123456}
⇒ x := 0; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {x → 0, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {x → 0, y → 123456}
⇒ s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {x → 0, y → 123456}
⇒ skip; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 0, y → 123456}
⇒ xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 0, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 0, xp → 0, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 0, xp → 0, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 0, xp → 0, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 0, xp → 1, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 0, xp → 1, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 0, xp → 1, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 0, xp → 1, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 0, xp → 1, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 0, xp → 2, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 0, xp → 2, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 0, xp → 2, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 0, xp → 2, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 0, xp → 2, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 0, xp → 4, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 0, xp → 4, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 0, xp → 4, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 0, xp → 4, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 0, xp → 4, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 0, xp → 8, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 0, xp → 8, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 0, xp → 8, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 0, xp → 8, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 0, xp → 8, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 0, xp → 16, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 0, xp → 16, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 0, xp → 16, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 0, xp → 16, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 0, xp → 16, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 0, xp → 32, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 0, xp → 32, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 0, xp → 32, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 0, xp → 32, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 0, xp → 32, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 0, xp → 64, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 0, xp → 64, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 0, xp → 64, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 0, xp → 64, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 0, xp → 64, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 0, xp → 128, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 0, xp → 128, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 256, x → 0, xp → 128, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 256, x → 0, xp → 128, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 256, x → 0, xp → 128, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 256, x → 0, xp → 256, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 256, x → 0, xp → 256, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 512, x → 0, xp → 256, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 512, x → 0, xp → 256, y → 123456}
⇒ skip; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 512, x → 0, xp → 256, y → 123456}
⇒ x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 512, x → 0, xp → 256, y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 512, x → 256, xp → 256, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 512, x → 256, xp → 256, y → 123456}
⇒ s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 512, x → 256, xp → 256, y → 123456}
⇒ skip; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 256, xp → 256, y → 123456}
⇒ xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 256, xp → 256, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 256, xp → 256, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 256, xp → 256, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 256, xp → 256, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 256, xp → 257, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 256, xp → 257, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 256, xp → 257, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 256, xp → 257, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 256, xp → 257, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 256, xp → 258, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 256, xp → 258, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 256, xp → 258, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 256, xp → 258, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 256, xp → 258, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 256, xp → 260, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 256, xp → 260, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 256, xp → 260, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 256, xp → 260, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 256, xp → 260, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 256, xp → 264, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 256, xp → 264, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 256, xp → 264, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 256, xp → 264, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 256, xp → 264, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 256, xp → 272, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 256, xp → 272, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 256, xp → 272, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 256, xp → 272, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 256, xp → 272, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 256, xp → 288, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 256, xp → 288, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 256, xp → 288, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 256, xp → 288, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 256, xp → 288, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 256, xp → 320, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 64, x → 256, xp → 320, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 256, xp → 320, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 256, xp → 320, y → 123456}
⇒ skip; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 256, xp → 320, y → 123456}
⇒ x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 256, xp → 320, y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 320, xp → 320, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 320, xp → 320, y → 123456}
⇒ s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 128, x → 320, xp → 320, y → 123456}
⇒ skip; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 320, xp → 320, y → 123456}
⇒ xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 320, xp → 320, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 320, xp → 320, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 320, xp → 320, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 320, xp → 320, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 320, xp → 321, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 320, xp → 321, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 320, xp → 321, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 320, xp → 321, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 320, xp → 321, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 320, xp → 322, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 320, xp → 322, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 320, xp → 322, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 320, xp → 322, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 320, xp → 322, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 320, xp → 324, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 320, xp → 324, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 320, xp → 324, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 320, xp → 324, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 320, xp → 324, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 320, xp → 328, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 320, xp → 328, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 320, xp → 328, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 320, xp → 328, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 320, xp → 328, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 320, xp → 336, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 320, xp → 336, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 320, xp → 336, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 320, xp → 336, y → 123456}
⇒ skip; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 320, xp → 336, y → 123456}
⇒ x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 320, xp → 336, y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 336, xp → 336, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 336, xp → 336, y → 123456}
⇒ s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 32, x → 336, xp → 336, y → 123456}
⇒ skip; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 336, xp → 336, y → 123456}
⇒ xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 336, xp → 336, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 336, xp → 336, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 336, xp → 336, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 336, xp → 336, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 336, xp → 337, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 336, xp → 337, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 336, xp → 337, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 336, xp → 337, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 336, xp → 337, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 336, xp → 338, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 336, xp → 338, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 336, xp → 338, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 336, xp → 338, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 336, xp → 338, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 336, xp → 340, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 336, xp → 340, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 336, xp → 340, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 336, xp → 340, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 336, xp → 340, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 336, xp → 344, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 336, xp → 344, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 336, xp → 344, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 336, xp → 344, y → 123456}
⇒ skip; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 336, xp → 344, y → 123456}
⇒ x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 336, xp → 344, y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 344, xp → 344, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 344, xp → 344, y → 123456}
⇒ s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 16, x → 344, xp → 344, y → 123456}
⇒ skip; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 344, xp → 344, y → 123456}
⇒ xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 344, xp → 344, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 344, xp → 344, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 344, xp → 344, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 344, xp → 344, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 344, xp → 345, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 344, xp → 345, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 344, xp → 345, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 344, xp → 345, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 344, xp → 345, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 344, xp → 346, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 344, xp → 346, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 344, xp → 346, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 344, xp → 346, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 344, xp → 346, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 344, xp → 348, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 344, xp → 348, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 344, xp → 348, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 344, xp → 348, y → 123456}
⇒ skip; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 344, xp → 348, y → 123456}
⇒ x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 344, xp → 348, y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 348, xp → 348, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 348, xp → 348, y → 123456}
⇒ s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 8, x → 348, xp → 348, y → 123456}
⇒ skip; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 348, xp → 348, y → 123456}
⇒ xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 348, xp → 348, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 348, xp → 348, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 348, xp → 348, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 348, xp → 348, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 348, xp → 349, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 348, xp → 349, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 348, xp → 349, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 348, xp → 349, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 348, xp → 349, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 348, xp → 350, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 348, xp → 350, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 348, xp → 350, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 348, xp → 350, y → 123456}
⇒ skip; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 348, xp → 350, y → 123456}
⇒ x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 348, xp → 350, y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 350, xp → 350, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 350, xp → 350, y → 123456}
⇒ s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 4, x → 350, xp → 350, y → 123456}
⇒ skip; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 350, xp → 350, y → 123456}
⇒ xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 350, xp → 350, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 350, xp → 350, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 350, xp → 350, y → 123456}
⇒ xp := (x+s); s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 350, xp → 350, y → 123456}
⇒ skip; s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 350, xp → 351, y → 123456}
⇒ s := (s*2); while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 1, x → 350, xp → 351, y → 123456}
⇒ skip; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 350, xp → 351, y → 123456}
⇒ while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 350, xp → 351, y → 123456}
⇒ skip; x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 350, xp → 351, y → 123456}
⇒ x := xp; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 350, xp → 351, y → 123456}
⇒ skip; while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 351, xp → 351, y → 123456}
⇒ while ¬(¬(y<(x*x))∧(y<((x+1)*(x+1)))) do { s := 1; xp := x; while (((x+s)*(x+s))<y) do { xp := (x+s); s := (s*2) }; x := xp }, {s → 2, x → 351, xp → 351, y → 123456}
⇒ skip, {s → 2, x → 351, xp → 351, y → 123456}' 
}

@test "algorithm-prime" {
  check 'r := 10; p := 1; i := 3; while i < r do { d := 0; j := 2; while d = 0 ∧ ¬(i < j*j) do { sum := j; while sum < i do sum := sum + j; if (sum = i) then d := 1 else skip; j := j + 1 }; if d = 0 then p := i else skip; i := i + 2 }' '⇒ skip; p := 1; i := 3; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {r → 10}
⇒ p := 1; i := 3; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {r → 10}
⇒ skip; i := 3; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {p → 1, r → 10}
⇒ i := 3; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {p → 1, r → 10}
⇒ skip; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {i → 3, p → 1, r → 10}
⇒ while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {i → 3, p → 1, r → 10}
⇒ d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {i → 3, p → 1, r → 10}
⇒ skip; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, p → 1, r → 10}
⇒ j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, p → 1, r → 10}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, j → 2, p → 1, r → 10}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, j → 2, p → 1, r → 10}
⇒ skip; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, j → 2, p → 1, r → 10}
⇒ if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, j → 2, p → 1, r → 10}
⇒ p := i; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, j → 2, p → 1, r → 10}
⇒ skip; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, j → 2, p → 3, r → 10}
⇒ i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 3, j → 2, p → 3, r → 10}
⇒ skip; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ skip; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 2}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 2}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 2}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 4}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 4}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 4}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 6}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 6}
⇒ skip; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 6}
⇒ if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 6}
⇒ skip; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 6}
⇒ j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 2, p → 3, r → 10, sum → 6}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 3, p → 3, r → 10, sum → 6}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 3, p → 3, r → 10, sum → 6}
⇒ skip; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 3, p → 3, r → 10, sum → 6}
⇒ if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 3, p → 3, r → 10, sum → 6}
⇒ p := i; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 3, p → 3, r → 10, sum → 6}
⇒ skip; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 3, p → 5, r → 10, sum → 6}
⇒ i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 5, j → 3, p → 5, r → 10, sum → 6}
⇒ skip; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 6}
⇒ while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 6}
⇒ d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 6}
⇒ skip; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 6}
⇒ j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 6}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 6}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 6}
⇒ sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 6}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 2}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 2}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 2}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 4}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 4}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 4}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 6}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 6}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 6}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 8}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 8}
⇒ skip; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 8}
⇒ if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 8}
⇒ skip; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 8}
⇒ j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 2, p → 5, r → 10, sum → 8}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 8}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 8}
⇒ skip; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 8}
⇒ if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 8}
⇒ p := i; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 5, r → 10, sum → 8}
⇒ skip; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 7, r → 10, sum → 8}
⇒ i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 7, j → 3, p → 7, r → 10, sum → 8}
⇒ skip; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 8}
⇒ while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 8}
⇒ d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 8}
⇒ skip; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 8}
⇒ j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 8}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 8}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 8}
⇒ sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 8}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 2}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 2}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 2}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 4}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 4}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 4}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 6}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 6}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 6}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 8}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 8}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 8}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 10}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 10}
⇒ skip; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 10}
⇒ if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 10}
⇒ skip; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 10}
⇒ j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 2, p → 7, r → 10, sum → 10}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 10}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 10}
⇒ sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 10}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 3}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 3}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 3}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 6}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 6}
⇒ sum := (sum+j); while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 6}
⇒ skip; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 9}
⇒ while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 9}
⇒ skip; if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 9}
⇒ if (sum=i) then { d := 1 } else { skip }; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 9}
⇒ d := 1; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 0, i → 9, j → 3, p → 7, r → 10, sum → 9}
⇒ skip; j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 3, p → 7, r → 10, sum → 9}
⇒ j := (j+1); while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 3, p → 7, r → 10, sum → 9}
⇒ skip; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 4, p → 7, r → 10, sum → 9}
⇒ while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 4, p → 7, r → 10, sum → 9}
⇒ skip; if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 4, p → 7, r → 10, sum → 9}
⇒ if (d=0) then { p := i } else { skip }; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 4, p → 7, r → 10, sum → 9}
⇒ skip; i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 4, p → 7, r → 10, sum → 9}
⇒ i := (i+2); while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 9, j → 4, p → 7, r → 10, sum → 9}
⇒ skip; while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 11, j → 4, p → 7, r → 10, sum → 9}
⇒ while (i<r) do { d := 0; j := 2; while ((d=0)∧¬(i<(j*j))) do { sum := j; while (sum<i) do { sum := (sum+j) }; if (sum=i) then { d := 1 } else { skip }; j := (j+1) }; if (d=0) then { p := i } else { skip }; i := (i+2) }, {d → 1, i → 11, j → 4, p → 7, r → 10, sum → 9}
⇒ skip, {d → 1, i → 11, j → 4, p → 7, r → 10, sum → 9}'
}

