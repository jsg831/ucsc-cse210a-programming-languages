# HW1 - ARITH

## Requirements
- GHC: it compiles Haskell code and supports modules used in this program

## How to Run
1. Run `make` to generate `arith` executable
2. Run `./arith` or `./test.sh` after merging with the test code
